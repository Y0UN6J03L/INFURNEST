// products.js - Product listing and search functionality for shop pages
// Loaded on non-shop pages but currently minimal/no-op

// Placeholder for future product catalog features
console.log('products.js loaded (minimal version)');

// Export functions if needed for shop integration
window.productUtils = {
  init: () => {
    // Initialize product-related features
    console.log('Product utils initialized');
  }
};

